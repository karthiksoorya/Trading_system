Nifty Trading System — TODO
Last updated: September 9, 2026
============================================================

✅ DONE
------------------------------------------------------------
[x] Config + broker adapter (Kite)
[x] Candle classification (boring/exciting)
[x] Zone detection (DBR/RBR/RBD/DBD)
[x] Booster scoring (freshness/strength/time/RR)
[x] Multi-timeframe confluence (5min + 15min + 60min)
[x] Position sizing (S.E.T.S — 1% risk/day)
[x] SQLite journal with CSV export
[x] Streamlit dashboard (Engine / Approvals / Signals / Performance)
[x] Approval flow (pending → approved → closed/rejected)
[x] Auto-exit on target / SL hit (monitor every 1 min)
[x] EOD auto-close at 15:20
[x] One active trade at a time (gate in Approvals tab)
[x] Today-only approvals (stale signals auto-rejected on startup)
[x] Telegram notifications (signal / approve / close / EOD summary)
[x] SL buffer configurable via UI slider (settings.json)
[x] Scan timeframe filter (multiselect in Settings)
[x] Zone class filter (demand/supply toggle in Settings)
[x] Engine auto-stop at 15:35
[x] Holiday detection (15 min frozen LTP → auto-stop)
[x] Stop Engine flag via settings.json (works under systemd)
[x] Approval gate fix (status='closed' authoritative)
[x] VPS deployment (running on AWS Lightsail)
[x] .env for credentials (gitignored)
[x] Daily CSV export with date stamp
[x] Performance tab (cumulative P&L, daily chart, zone/TF breakdown)
[x] Breakeven SL (move SL to entry at 1:1 R:R) — Aug 2026
[x] Full bug fix pass — 22 software bugs fixed — Aug 2026 (see FIXES.md)
[x] Trading logic fixes — 8 issues fixed — Aug 2026 (see FIXES.md)
[x] VIX filter — skip scan when India VIX > 20 — Aug 2026
[x] Opposing zone target cap — don't set target inside resistance — Aug 2026
[x] SCAN_START moved to 10:15 — avoid opening volatility zones — Aug 2026
[x] Expiry buffer — skip contracts within 2 days of expiry — Aug 2026
[x] Time exit at 13:00 — close if target not hit by afternoon — Aug 2026
[x] Options P&L tracking — real rupee P&L in EOD summary and close msgs — Aug 2026
[x] Options trailing stop (30%) — exits when premium up 30% — Aug 2026
     NOTE: Review after 1 week — may replace with index 75% partial exit


⏳ PENDING — CRITICAL (from 3-year backtest, Aug 29 2026)
------------------------------------------------------------
[ ] Limit-order entry at the zone proximal  → docs/LIMIT_ENTRY_DESIGN.md
      → BIGGEST lever. Backtest: market-order-on-approval loses ~4,300 index
         points / 3 years vs a limit order resting at the proximal.
      → "Armed" signal state: on approve, poll the index; fire the option
         order only when Nifty touches the proximal; cancel after
         SIGNAL_EXPIRY_MINUTES or if the zone breaks.
      → Behind ENTRY_MODE setting, paper first. Do NOT ship same-day with
         any other live change.
      → Files: journal/db.py, telegram_handler.py, scheduler.py, config.py, app.py

[x] PE-only + high-quality-only config   (DONE Aug 29, commit 2f9cc13)
      → SCAN_ZONE_CLASSES=supply (CE had -245 pts/3y), MIN_BOOSTER_SCORE=10,
         MIN_CONFLUENCE=3, MIN_RISK_POINTS=15 (new), ZONE_APPROACH=30,
         MAX_TRADES_PER_DAY=2. Also set these in dashboard Settings (settings.json
         overrides config.py on the VPS).

[x] Backtest option SPREADS vs naked PE  (CLOSED Aug 29 - margin math settles it)
      → Ran Zerodha basket_order_margins (SPAN backend) on live account, NIFTY 24175:
         naked long PE  = Rs 6,113 blocked (= premium, already defined-risk)
         bear put spread = Rs 34,256 blocked, max loss Rs 2,828, max profit Rs 3,673
         deep-ITM debit  = Rs 36,167 blocked, tiny max profit
         NIFTY futures   = Rs 1,77,531 (> whole account; F&O MIS leverage is gone)
      → Verdict: naked long PE is ALREADY capped-risk (max loss = premium). The
         spread ties up ~Rs28k MORE capital to shave Rs3k off a worst-case the
         index SL already prevents, and caps upside. Bad trade. Stay with naked PE.

[ ] Grow the per-trade edge above the cost floor
      → Even the strict config: +3.5 idx pts/trade, still -Rs308/trade after
         Zerodha costs (STT ~Rs312 on notional).
      → Options loss was quoted Rs950-1400/trade but the option model was
         Rs1,900/leg OPTIMISTIC (fixed Sep 9). RE-RUN done Sep 9:
         - strict config: only 4 trades / 3yr (no-trade failure), all losers
         - loose sample (score>=7 conf>=2, n=76): futures -Rs575/trade,
           opt_itm1 -Rs1,276/trade win 3.9%, even the 11 target-hits avg -Rs452
         => long option buying not viable at any strike/config with honest
            pricing. At best a futures edge, and that's -Rs575/trade on costs
            in the loose config. Strict config fires too rarely to validate.
      → Options: wider targets, or only trade when the move is expected to be
         big (news, opening range break), or accept it's a futures-only edge.

[ ] BACKTEST DATA REALISM — option P&L is currently synthetic (see AGENT_KNOWLEDGE 11.1)
      → backtest/option_model.py prices options with Black-Scholes + a VIX->IV
         model calibrated on ONLY 14 real trades. ~Rs150/trade optimistic,
         +/-Rs300-400 per-trade error. The rupee bottom line is an estimate.
         Index/futures-points numbers are fine — distrust the option P&L only.
      → Step 1 [BUILT Sep 9] backtest/data/nse_bhavcopy.py (downloader, both
         formats; liquid_marks() shared universe) + validate_option_model.py
         (model vs real CLOSE) + calibrate.py rewritten two-stage:
           Stage A = static surface vs bhavcopy marks, with a 70/30 time split
                     (fit early days, score held-out later days -> Step 3 done)
           Stage B = crush_coef + half_spread_pts vs the 14 live trades only
         RUN THE FULL PULL:  py -3.14 -m backtest.data.nse_bhavcopy --years 3
         then                py -3.14 -m backtest.validate_option_model
         then                py -3.14 -m backtest.calibrate
         then paste the fitted ModelParams block into backtest/option_model.py
         DONE Sep 9 on full 3yr (738 days, 1.19M rows, 56,696 marks):
         OLD defaults were +Rs1,900/leg OPTIMISTIC across all 12 quarters ->
         real options loss was ~Rs2,800-3,300/trade not Rs950-1,400. option_model
         ModelParams updated: term_mult 1.30->1.00, skew 0.90->2.20, crush 0.70->
         1.00, half_spread 1.5->2.5. Held-out bias now +Rs48-156/leg (generalises).
         Re-run calibrate after each fresh bhavcopy pull.
      → Step 2 (paid / vendor account, NOT just a login): minute-level option
         data, 2-3 yrs Nifty weeklies with real bid/ask. Options: GDFL or
         TrueData (~Rs1-2k/mo + API creds); Kite historical add-on (Rs2000/mo +
         historical instrument master for expired tokens); or Upstox historical
         API (has expired-option candles, cheapest, pairs with Upstox adapter).
         [USER: planning Upstox]
      → Step 3 [DONE in Step 1] held-out time split is built into calibrate.py
         Stage A. Stage B still fits+evaluates on the same 14 trades (too few to
         split) — keep that caveat in mind.
      → Step 4: random-entry benchmark — same timestamps, random direction —
         to prove the zones add value.
      → Step 5: re-run the edge test on real premiums + real costs, then decide
         if the supply/demand edge is real.

[x] Settings sweep for the Aug29-Sep11 no-trade stall (Sep 11) — CLOSED, no knob helps
      → Refreshed Kite token + candle cache (fetch.py OVERWRITES not merges — always
         use --years 3, never a shorter window, or you truncate the option-model
         calibration history. Happened once Sep 11, caught and fixed same session.)
      → confluence 2 (live) on the stall window: 0 trades, matches live exactly (sanity ok)
      → approach 30->60, or dropping trend filter, alone: still 0 trades — confluence is
         the actual gate, not approach/trend
      → confluence 1, full 3yr, n=163: futures -Rs642/trade win 36% PF 0.57, opt_itm1
         -Rs1,266/trade win 3.7% PF 0.02, index net -375pts. WORSE than confluence 2,
         not better -> loosening confluence adds losing trades, doesn't unlock edge.
      → Conclusion: confluence=2 is correctly filtering. The stall was a genuinely thin
         fortnight, not a config bug. No settings change is warranted from this test.
      → Also: slippage model is a flat 1.5pt half-spread (no widening at
         open/close/expiry/fast moves); no regime segmentation of results;
         no event calendar.


⏳ PENDING — HIGH PRIORITY (build after 1 week of live data)
------------------------------------------------------------
[ ] Replace options trail with index 75% partial exit
      → If index has moved 75% toward target AND time > 11:30, exit
      → Keeps all logic index-based (not chasing options price)
      → Example: entry 24201, target 24266 → exit at 24250 after 11:30
      → File: engine/signals.py + scheduler.py monitor_open_trades

[~] Curve analysis — HTF position filter — BUILT Sep 11 as backtest opt-in, PROMISING NOT PROVEN
      → engine/curve.py (pure fn, no I/O) + MarketData.daily_range() (20-day
         lookback, no-lookahead) + DSParams.curve_filter (default OFF) wired
         into strategy_ds.py + backtest/run.py --curve-filter flag.
      → Skips demand zones in top 25% of the 20-day range, supply in bottom 25%.
      → Full 3yr (score8/conf2, matches live): idx pts -36 -> +151, futures
         -Rs508/trade -> -Rs182/trade, win 37.8% -> 43.2%, PF 0.63 -> 0.84.
         Options still lose (spread/theta dominates regardless).
      → SPLIT BY HALF — NOT stable: 2023-25 +143pts/PF1.10 (carries the whole
         result); 2025-26 (incl. this week's downtrend regime) only +8pts/PF0.66,
         net LOSS. Do NOT deploy live off this alone.
      → Next: proper walk-forward (rolling fit/validate, not one static split)
         before trusting it. Extreme=0.25 beat 0.35 in the one comparison run
         (+151 vs +108 pts) — not swept further, don't over-tune on this sample.
      → Files: engine/curve.py, backtest/marketdata.py, backtest/strategy_ds.py,
         backtest/replay.py, backtest/run.py

[ ] HPA levels — High Probability Areas
      → Previous Day High (PDH), Previous Day Low (PDL)
      → Opening High (CDH), Opening Low (CDL)
      → Big Round Numbers (24000, 24100, 24200, etc.)
      → If target sits just beyond a round number, it rarely hits
      → Check: is there an HPA between entry and target? If yes, cap target there.
      → File: engine/signals.py (extend opposing zone cap)

[ ] Approval delay tracking   (mostly superseded by the limit-entry change above)
      → Log approved_time in DB (new column)
      → Backtest CONFIRMED: 2-5 min late entry converts +366 pts/3y into -3,941.
      → If limit-entry is built, this becomes just a diagnostic. Until then,
         flag delay > 2 min in Telegram.
      → File: journal/db.py (add approved_at column), telegram_handler.py


⏳ PENDING — MEDIUM PRIORITY
------------------------------------------------------------
[ ] First Trouble Area (FTA) check
      → Before generating a signal, check: is there a valid zone between
         entry and target that price must pass through?
      → If yes, the target is harder to reach — reduce confidence or skip
      → The opposing zone cap (Fix D) is a start but FTA is more nuanced
      → File: engine/signals.py generate_signal()

[ ] Opening bias filter
      → If market opens with gap down > 0.3% vs previous close → bearish bias
      → Skip demand zone signals on bearish-bias days
      → Skip supply zone signals on bullish-bias days
      → File: scheduler.py _scan_core()

[ ] Multi-day zone tracking
      → Zones that formed yesterday but weren't approached are often stronger
      → Currently scans days=5 of history but doesn't weight older zones lower
      → Add: zone age scoring — fresher formation = higher weight
      → File: engine/boosters.py score_freshness()

[ ] Kite daily token via Telegram
      → Bot sends login link to Telegram every morning at 8:30 AM
      → You click, paste redirect URL back to bot
      → Bot saves token automatically

[ ] VPS systemd auto-start
      → Engine should restart automatically if VPS reboots
      → Create /etc/systemd/system/nifty-engine.service


⏳ PENDING — LOW PRIORITY
------------------------------------------------------------
[ ] Bank Nifty as second instrument
      → Add BANKNIFTY_SYMBOL to config
      → Friday expiry (check current NSE schedule)
      → Same zone/signal logic applies

[ ] Trailing SL (let winners run)
      → After breakeven, instead of fixed target, trail SL by N points
      → Captures extended moves beyond 2× R:R

[ ] Position verification before SELL
      → Before placing exit order, verify position exists on Kite
      → Prevents naked short if manually closed outside system
      → File: brokers/kite_adapter.py + scheduler.py _live_exit()

[ ] Upstox adapter
      → Implement when Upstox API is enabled on account


📊 THIS WEEK — OBSERVE AND RECORD (Aug 21–28, 2026)
------------------------------------------------------------
Don't change code. Gather data to answer:

[ ] At what time does options premium peak vs when index hits its high?
[ ] How much does theta take per hour after 12:00? (record CE price at 12:00, 13:00, 14:00)
[ ] Is 13:00 the right time cutoff or should it be 12:30?
[ ] Is 30% options trail correct or does it exit too early / too late?
[ ] Which zone types (DBR/RBR/RBD/DBD) fired and which actually worked?
[ ] What was VIX at each signal entry? Is 20 the right threshold?
[ ] How many minutes between signal and approval? Is late entry hurting?
[ ] Does the 60min trend filter correctly block bad trades?

After each trade: write 2 lines — what index did, what options did.
This becomes training data for better rules.


🎯 VALIDATION CHECKLIST
------------------------------------------------------------
[x] System running live on VPS
[x] Telegram approve/reject/close working
[x] Auto-exit on target and SL working
[x] Breakeven SL working
[x] EOD auto-close working
[ ] 10+ live trades completed — check autolearn data
[ ] Win rate > 45% over 20+ trades
[ ] Options P&L positive over 1 week
[ ] No system crashes for 5 consecutive trading days
============================================================
